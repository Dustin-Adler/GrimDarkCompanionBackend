<%= form_with(model: terrain_layout) do |form| %>
  <% if terrain_layout.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(terrain_layout.errors.count, "error") %> prohibited this terrain_layout from being saved:</h2>

      <ul>
        <% terrain_layout.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :name, style: "display: block" %>
    <%= form.text_field :name %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
