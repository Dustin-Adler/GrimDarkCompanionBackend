<%= form_with(model: available_strategem) do |form| %>
  <% if available_strategem.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(available_strategem.errors.count, "error") %> prohibited this available_strategem from being saved:</h2>

      <ul>
        <% available_strategem.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :model_id, style: "display: block" %>
    <%= form.number_field :model_id %>
  </div>

  <div>
    <%= form.label :strategem_id, style: "display: block" %>
    <%= form.number_field :strategem_id %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
