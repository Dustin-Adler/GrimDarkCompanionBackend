<%= form_with(model: weapon_ability) do |form| %>
  <% if weapon_ability.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(weapon_ability.errors.count, "error") %> prohibited this weapon_ability from being saved:</h2>

      <ul>
        <% weapon_ability.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :weapon_id, style: "display: block" %>
    <%= form.number_field :weapon_id %>
  </div>

  <div>
    <%= form.label :ability_id, style: "display: block" %>
    <%= form.number_field :ability_id %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
