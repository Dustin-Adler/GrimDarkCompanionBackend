<%= form_with(model: detachment) do |form| %>
  <% if detachment.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(detachment.errors.count, "error") %> prohibited this detachment from being saved:</h2>

      <ul>
        <% detachment.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :name, style: "display: block" %>
    <%= form.text_field :name %>
  </div>

  <div>
    <%= form.label :description, style: "display: block" %>
    <%= form.text_field :description %>
  </div>

  <div>
    <%= form.label :army_id, style: "display: block" %>
    <%= form.number_field :army_id %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
