<%= form_with(model: model_weapon) do |form| %>
  <% if model_weapon.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(model_weapon.errors.count, "error") %> prohibited this model_weapon from being saved:</h2>

      <ul>
        <% model_weapon.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :model_id, style: "display: block" %>
    <%= form.number_field :model_id %>
  </div>

  <div>
    <%= form.label :weapon_id, style: "display: block" %>
    <%= form.number_field :weapon_id %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
