<%= form_with(model: mission_rule) do |form| %>
  <% if mission_rule.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(mission_rule.errors.count, "error") %> prohibited this mission_rule from being saved:</h2>

      <ul>
        <% mission_rule.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :name, style: "display: block" %>
    <%= form.text_field :name %>
  </div>

  <div>
    <%= form.label :description, style: "display: block" %>
    <%= form.text_field :description %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
