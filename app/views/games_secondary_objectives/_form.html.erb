<%= form_with(model: games_secondary_objective) do |form| %>
  <% if games_secondary_objective.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(games_secondary_objective.errors.count, "error") %> prohibited this games_secondary_objective from being saved:</h2>

      <ul>
        <% games_secondary_objective.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :game_id, style: "display: block" %>
    <%= form.number_field :game_id %>
  </div>

  <div>
    <%= form.label :player_id, style: "display: block" %>
    <%= form.number_field :player_id %>
  </div>

  <div>
    <%= form.label :secondary_objective_id, style: "display: block" %>
    <%= form.number_field :secondary_objective_id %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
